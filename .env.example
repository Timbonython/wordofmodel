# Copy to .env.local. Never commit the filled in version: .env* is gitignored.
# The same set goes in Vercel -> Project -> Settings -> Environment Variables.

OPENAI_API_KEY=sk-...
PERPLEXITY_API_KEY=pplx-...

# Server side only. The secret key bypasses RLS, which is what makes the
# no-policy setup in supabase/migrations/0001_init.sql work. It must never be
# exposed to a browser.
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_SECRET_KEY=sb_secret_...

# Used only by the server side auth client in lib/auth.ts, which is what makes the
# magic link work. It is RLS bound and safe to expose, but nothing in this build
# talks to Supabase from a browser, so it is deliberately not NEXT_PUBLIC_.
SUPABASE_PUBLISHABLE_KEY=sb_publishable_...

RESEND_API_KEY=re_...
RESEND_FROM="Word of Model <results@wordofmodel.ai>"
RESEND_REPLY_TO=hello@wordofmodel.ai

# Salt for hashing visitor IPs. Required, no fallback: a missing value now takes
# the scan routes down rather than quietly reusing the Supabase secret. Generate
# one per environment with:  openssl rand -hex 32
# Changing it orphans every hash already stored, so set it once and leave it.
IP_HASH_SALT=

# Production only. Deliberately NOT set on Preview: with no value there,
# lib/env.ts derives the base URL from VERCEL_URL, so a preview deploy's Stripe
# redirects and magic links come back to the deploy that is actually running
# rather than to the live site. Unset locally gives http://localhost:3000.
NEXT_PUBLIC_SITE_URL=https://wordofmodel.ai

# Model overrides, so a bad model release can be rolled back without a deploy.
# Defaults are set in lib/env.ts and were verified against both live APIs.
# OPENAI_MODEL_ANSWER=gpt-5.5
# OPENAI_MODEL_UTILITY=gpt-5.4-mini
# PERPLEXITY_MODEL=perplexity/sonar

# Stripe. Test mode for this build: STRIPE_MODE guards it, and lib/stripe.ts
# refuses to start on a mismatch between the mode and the key prefix.
STRIPE_MODE=test
STRIPE_SECRET_KEY=sk_test_...

# From `stripe listen --forward-to localhost:3000/api/stripe/webhook` locally,
# or from the endpoint's signing secret in the dashboard.
# NO PRICE IDS. STRIPE_PRICE_FOUNDING_MONTHLY and STRIPE_PRICE_STANDARD_MONTHLY were removed on
# 28 Aug 2026: every price now carries a Stripe lookup_key and lib/stripe.ts resolves ids through
# those. An id differs between test and live mode and a lookup key does not, so there is nothing
# left here to set to the wrong mode's value.
STRIPE_WEBHOOK_SECRET=whsec_...

# Created by `npm run stripe:setup`, which prints these. Pinned by id rather
# than looked up by key at runtime, so a price cannot be swapped underneath a
# live checkout.

# Optional. Without it Stripe falls back to the account default portal
# configuration, which has plan switching on, and plan switching off a founding
# price is the one thing the portal must not allow.
# STRIPE_PORTAL_CONFIGURATION_ID=bpc_...

# Where failed payments, held reports and every other ops alert get reported.
# Without it an alert only reaches the server log, which is revenue leaking quietly.
#
# THE ALERT CHANNEL NEVER LIVES ON THE DOMAIN IT MONITORS. It was hello@wordofmodel.ai
# until 21 Aug 2026, which is also the reply-to on every subscriber email and is routed by
# Cloudflare Email Routing. One bad routing rule takes out the address a customer replies
# to and the address that would have told us about it, from a single cause: that address
# bounced 550 5.1.1 three times on 17 Aug, rejected by Cloudflare's own MX. lib/env.ts
# warns if this is ever moved back onto the sending domain.
#
# A personal Gmail rather than a Frame address, and that is deliberate twice over. Word of
# Model is being kept outside Frame, so its operational mail should not be entangled with
# Frame's either - and Gmail already gives what Frame would: a different provider, a
# different domain, a different failure mode.
ALERT_EMAIL=therealtimpearce@gmail.com

# Whether the wizard is offered to visitors. Off, the pricing block and the scan
# result keep the waitlist they had before onboarding existed, because a visitor
# sent to a test mode Checkout gets a page that will not take their card.
#
# It gates the public CTAs and nothing else: /start stays reachable by URL and
# every wizard route keeps working, so the whole flow including the webhook can
# be walked on production while visitors still see the waitlist.
#
# Defaults to false. Set true on production when the live keys are in.
WIZARD_LIVE=true

# Meta advertising. Both unset by default, so forgetting them means no pixel is served and no
# server event is sent - the failure direction is always less tracking, never more.
#
# The pixel is not served to visitors from the UK, the EEA or Switzerland at all, and the
# privacy policy says so. That is a promise in published copy: read lib/meta.ts before
# changing how it is decided.
META_PIXEL_ID=
META_CAPI_TOKEN=

# ---------------------------------------------------------------- reviews (all optional)
#
# WHERE A REVIEWER CAN ALSO POST, once those listings exist. Every one is unset until somebody
# creates the listing, and a platform with no URL is not rendered at all - a button that goes
# nowhere is worse than an absent one.
#
# REVIEW_URL_GOOGLE     needs a Google Business Profile. From the profile: Ask for reviews ->
#                       the short g.page/r/... link, which opens the review box directly.
# REVIEW_URL_G2         needs a claimed G2 product page. The form is
#                       https://www.g2.com/products/<slug>/reviews/start
# REVIEW_URL_TRUSTPILOT needs a claimed Trustpilot business page. The form is
#                       https://www.trustpilot.com/evaluate/wordofmodel.ai
REVIEW_URL_GOOGLE=
REVIEW_URL_G2=
REVIEW_URL_TRUSTPILOT=
